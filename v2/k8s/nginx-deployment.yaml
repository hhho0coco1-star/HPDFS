apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: pdfs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: pdfs-nginx:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
